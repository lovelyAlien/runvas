spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

runvas:
  jwt:
    secret: dev-secret-dev-secret-dev-secret-dev-secret
    expiration-seconds: 3600
